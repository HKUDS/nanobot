# 文件监控系统集成指南

## 📋 集成概述

文件监控系统已经实现并测试完成，现在需要将其集成到现有的Power机制和BFF服务中。

## 🔧 集成步骤

### 1. 修改 BFF 服务导入文件监控

在 `bff/bff_service.py` 中添加导入：

```python
# 在文件顶部添加导入
from shared.file_monitor import ConversationFileMonitor

# 创建全局文件监控器实例
conversation_monitor = ConversationFileMonitor()
```

### 2. 扩展 conversations 数据结构

在现有的 `conversations` 字典中添加文件监控相关字段：

```python
conversations[conv_id] = {
    # ... 原有字段 ...
    "power": 50.0,
    "power_history": [],
    "file_stats": {},  # 新增：文件统计信息
    "last_file_check": None,  # 新增：最后检查时间
}
```

### 3. 在关键操作点集成文件监控

#### 3.1 对话创建时初始化监控

在创建新对话时，初始化文件监控：

```python
@app.post("/conversations", response_model=ConversationCreateResponse)
async def create_conversation(req: ConversationCreate):
    # ... 原有逻辑 ...
    
    # 初始化文件监控
    files = conversation_monitor.get_conversation_files(conversation_id)
    for file_path in files:
        conversation_monitor.monitor.take_snapshot(file_path)
    
    # 记录初始文件状态
    conversations[conversation_id]["file_stats"] = conversation_monitor.get_conversation_stats(conversation_id)
    conversations[conversation_id]["last_file_check"] = datetime.now(TZ_UTC8).isoformat()
    
    return ConversationCreateResponse(conversation_id=conversation_id)
```

#### 3.2 消息发送时监控文件变化

在 `/chat` 接口中集成文件监控：

```python
@app.post("/conversations/{conversation_id}/messages", response_model=MessageResponse)
async def send_message(conversation_id: str, req: MessageSend):
    # ... 原有逻辑 ...
    
    result = resp.json()
    
    # 监控文件变化
    changes = conversation_monitor.monitor_conversation(conversation_id)
    
    # 记录文件变化到对话状态
    conversations[conversation_id]["file_stats"] = conversation_monitor.get_conversation_stats(conversation_id)
    conversations[conversation_id]["last_file_check"] = datetime.now(TZ_UTC8).isoformat()
    
    # 如果有文件变化，记录到Power更新原因中
    file_changes_summary = {}
    for file_path, change in changes.items():
        if change.status != "unchanged":
            filename = Path(file_path).name
            file_changes_summary[filename] = {
                "status": change.status,
                "size_delta": change.size_delta,
                "lines_delta": change.lines_delta
            }
    
    # 提取reward并更新Power（带文件变化信息）
    trajectory = result.get("trajectory", [])
    if trajectory:
        last_r = trajectory[-1].get("r_t", 0.5)
        
        # 更新Power，包含文件变化信息
        await update_power_with_file_monitoring(
            conversation_id, 
            last_r, 
            file_changes_summary
        )
    
    return MessageResponse(**result)
```

### 4. 创建带文件监控的Power更新函数

```python
async def update_power_with_file_monitoring(conversation_id: str, reward: float, file_changes: dict):
    """带文件监控的Power更新"""
    async with conversations_lock:
        conv = conversations.get(conversation_id)
        if not conv:
            return
        
        old_power = conv["power"]
        delta = power_calc.calculate_delta(reward_norm=reward)
        new_power = power_calc.update_power(old_power, delta)
        
        # 构建更新原因（包含文件变化信息）
        reason = f"auto_reward_{reward}"
        if file_changes:
            file_info = ", ".join([f"{k}:{v['status']}" for k, v in file_changes.items()])
            reason = f"{reason} (files: {file_info})"
        
        conv["power"] = new_power
        conv["power_history"].append({
            "timestamp": datetime.now(TZ_UTC8).isoformat(),
            "value": new_power,
            "reason": reason,
            "file_changes": file_changes  # 新增：记录文件变化
        })
        
        # 记录到文件监控日志
        conversation_monitor.log_conversation_changes(
            conversation_id,
            "power_update",
            f"Power updated: {old_power:.1f} -> {new_power:.1f} (Δ{delta:+.1f})"
        )
```

### 5. 添加文件监控API接口

```python
@app.get("/conversations/{conversation_id}/files")
async def get_conversation_files(conversation_id: str):
    """获取对话文件状态"""
    conv = conversations.get(conversation_id)
    if not conv:
        raise HTTPException(404)
    
    return {
        "file_stats": conv.get("file_stats", {}),
        "last_file_check": conv.get("last_file_check"),
        "monitored_files": conversation_monitor.key_files
    }

@app.get("/conversations/{conversation_id}/file-changes")
async def get_file_changes(conversation_id: str, limit: int = 20):
    """获取文件变化历史"""
    # 从文件监控器获取变化历史
    files = conversation_monitor.get_conversation_files(conversation_id)
    
    changes_history = {}
    for file_path in files:
        history = conversation_monitor.monitor.get_change_history(file_path, limit)
        filename = Path(file_path).name
        changes_history[filename] = history
    
    return {"file_changes": changes_history}
```

### 6. 前端集成

在前端添加文件监控面板：

```vue
<template>
  <div class="file-monitor-panel">
    <h4>文件监控</h4>
    <div v-for="(stat, filename) in fileStats" :key="filename" class="file-stat">
      <div class="file-header">
        <span class="filename">{{ filename }}</span>
        <span v-if="stat.exists" class="file-size">{{ stat.size }} bytes</span>
        <span v-else class="file-missing">文件不存在</span>
      </div>
      <div v-if="stat.exists" class="file-details">
        <span>行数: {{ stat.lines }}</span>
        <span>修改时间: {{ formatTime(stat.mtime) }}</span>
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'

const fileStats = ref({})

const fetchFileStats = async (conversationId) => {
  const response = await fetch(`/conversations/${conversationId}/files`)
  const data = await response.json()
  fileStats.value = data.file_stats
}

onMounted(() => {
  // 每30秒更新文件状态
  setInterval(() => {
    const convId = getCurrentConversationId()
    if (convId) {
      fetchFileStats(convId)
    }
  }, 30000)
})
</script>
```

## 🎯 集成优势

### 1. **完整的可观测性**
- 每个Power更新都关联文件变化
- 详细的文件状态跟踪
- 变化历史记录

### 2. **调试友好**
- 快速定位文件相关的问题
- 监控轨迹记录是否正常
- 检测记忆文件更新

### 3. **性能优化**
- 高效的监控算法
- 最小化系统开销
- 支持大规模部署

## 🔧 测试验证

集成完成后，运行以下测试：

1. **基础功能测试**: 确保文件监控不影响现有功能
2. **性能测试**: 验证监控对系统性能的影响
3. **集成测试**: 测试Power更新与文件监控的联动

## 📊 监控指标

集成后可以监控的关键指标：
- 轨迹文件大小变化趋势
- 记忆文件更新频率
- Power变化与文件变化的关联性
- 系统性能影响

这个集成方案将文件监控系统无缝集成到现有的Power机制中，提供完整的可观测性支持！