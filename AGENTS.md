# Repository Instructions

- 在本仓库中，进行较大代码变更前，先输出并确认执行计划（plan），再开始编码。
- 在本仓库中，每次新增一个功能后，先完成对应测试并确认通过，再提交一次 commit。
- 启动 `nanobot gateway` 前，先确认当前机器仅有 0 或 1 个 gateway 进程；若发现多个实例，先停止多余实例后再启动，确保全程仅运行一个实例。
- `token-guard` 仅在用户明确要求，或会话出现明显高风险 token 模式时使用；普通任务不要默认启用。
