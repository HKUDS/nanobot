# Orghi Change Tracking: fix/

Tracking artifacts for orghi bug fixes. Used for merge conflict resolution when syncing upstream.

Each `<slug>.md` file (from branch `fix/<slug>`) documents:
- Files touched
- Purpose, fixes, improves
- Conflict resolution guidance
- Worst case: accept upstream, rebuild steps

Created by the **orghi-change-tracker** agent after fix completion.
