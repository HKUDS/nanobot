# Orghi Change Tracking: feat/

Tracking artifacts for orghi features. Used for merge conflict resolution when syncing upstream.

Each `<slug>.md` file (from branch `feat/<slug>`) documents:
- Files touched
- Purpose, fixes, improves
- Conflict resolution guidance
- Worst case: accept upstream, rebuild steps

Created by the **orghi-change-tracker** agent after feature completion.
